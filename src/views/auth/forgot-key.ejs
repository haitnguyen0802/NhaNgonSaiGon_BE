<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quên mã đăng nhập | Nhà Ngon Sài Gòn</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            background-color: #f5f5f5;
        }
        .forgot-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .forgot-form {
            max-width: 450px;
            width: 100%;
            padding: 30px;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.08);
        }
        .forgot-logo {
            text-align: center;
            margin-bottom: 25px;
        }
        .forgot-logo img {
            max-width: 180px;
        }
        .form-floating {
            margin-bottom: 15px;
        }
        .btn-submit {
            width: 100%;
            padding: 12px;
            font-weight: 600;
        }
        .key-info {
            background-color: #f8f9fa;
            border-radius: 6px;
            padding: 15px;
            margin-bottom: 20px;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="forgot-container">
        <div class="forgot-form">
            <div class="forgot-logo">
                <img src="/img/logo.png" alt="Nhà Ngon Sài Gòn" onerror="this.src='/img/logo-placeholder.png'; this.onerror=null;">
                <h2 class="mt-3">Nhà Ngon Sài Gòn</h2>
                <p class="text-muted">Yêu cầu mã đăng nhập mới</p>
            </div>

            <div class="key-info">
                <p>Mã đăng nhập mới sẽ được gửi đến email quản trị viên đã được cấu hình. Mã cũ sẽ bị vô hiệu hóa.</p>
                <p>Mã đăng nhập mới sẽ có hiệu lực đến 0h00 ngày hôm sau.</p>
            </div>

            <% if (locals.messages && messages.error) { %>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <%= messages.error %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            <% } %>

            <% if (locals.messages && messages.success) { %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <%= messages.success %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            <% } %>

            <form action="/auth/forgot-key" method="POST">
                <p class="text-center mb-4">Bấm vào nút bên dưới để yêu cầu mã đăng nhập mới</p>
                <button type="submit" class="btn btn-primary btn-submit">Gửi mã đăng nhập mới</button>
            </form>
            
            <div class="mt-4 text-center">
                <a href="/auth/login" class="text-decoration-none"><i class="fas fa-arrow-left me-1"></i> Quay lại đăng nhập</a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Auto-close alerts after 5 seconds
        document.addEventListener('DOMContentLoaded', function() {
            setTimeout(function() {
                var alerts = document.querySelectorAll('.alert');
                alerts.forEach(function(alert) {
                    var bsAlert = new bootstrap.Alert(alert);
                    bsAlert.close();
                });
            }, 5000);
        });
    </script>
</body>
</html> 